package wrapper;

public class Test {
    public static void main(String[] args) {
       int a=3;//primitive type value no methods


       //a is stored in stack

//we have to use wrapper classes in collections  because the primitive classes does not support methods 

       //wrapper can perform all the work as primitive 
       //wrapper is object so it can point to null 
       //but primitive cant hold null values 
       //wrapper is object so data is stored in heap 
       Integer b=8;
       //autoboxing --directaly convert primitive into object 
       //but here  integer is a wrapper class which has wrapped a
       //  primitive type of data into object so that we can provide methods  to it  
    }
}

//java is not a purely object oriented language

//wrapper classes 
//